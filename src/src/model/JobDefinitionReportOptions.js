/**
 * Notehub API
 * The OpenAPI definition for the Notehub.io API.
 *
 * The version of the OpenAPI document: 1.2.0
 * Contact: engineering@blues.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from "../ApiClient";

/**
 * The JobDefinitionReportOptions model module.
 * @module model/JobDefinitionReportOptions
 * @version 6.4.0
 */
class JobDefinitionReportOptions {
  /**
   * Constructs a new <code>JobDefinitionReportOptions</code>.
   * Controls what data is included in the job report
   * @alias module:model/JobDefinitionReportOptions
   */
  constructor() {
    JobDefinitionReportOptions.initialize(this);
  }

  /**
   * Initializes the fields of this object.
   * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
   * Only for internal use.
   */
  static initialize(obj) {}

  /**
   * Constructs a <code>JobDefinitionReportOptions</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:model/JobDefinitionReportOptions} obj Optional instance to populate.
   * @return {module:model/JobDefinitionReportOptions} The populated <code>JobDefinitionReportOptions</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      obj = obj || new JobDefinitionReportOptions();

      if (data.hasOwnProperty("app_fleets")) {
        obj["app_fleets"] = ApiClient.convertToType(
          data["app_fleets"],
          "Boolean"
        );
      }
      if (data.hasOwnProperty("app_info")) {
        obj["app_info"] = ApiClient.convertToType(data["app_info"], "Boolean");
      }
      if (data.hasOwnProperty("app_vars")) {
        obj["app_vars"] = ApiClient.convertToType(data["app_vars"], "Boolean");
      }
      if (data.hasOwnProperty("comment")) {
        obj["comment"] = ApiClient.convertToType(data["comment"], "String");
      }
      if (data.hasOwnProperty("device_activity")) {
        obj["device_activity"] = ApiClient.convertToType(
          data["device_activity"],
          "Boolean"
        );
      }
      if (data.hasOwnProperty("device_health")) {
        obj["device_health"] = ApiClient.convertToType(
          data["device_health"],
          "Boolean"
        );
      }
      if (data.hasOwnProperty("device_info")) {
        obj["device_info"] = ApiClient.convertToType(
          data["device_info"],
          "Boolean"
        );
      }
      if (data.hasOwnProperty("device_vars")) {
        obj["device_vars"] = ApiClient.convertToType(
          data["device_vars"],
          "Boolean"
        );
      }
    } else if (data === null) {
      return null;
    }
    return obj;
  }

  /**
   * Validates the JSON data with respect to <code>JobDefinitionReportOptions</code>.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @return {boolean} to indicate whether the JSON data is valid with respect to <code>JobDefinitionReportOptions</code>.
   */
  static validateJSON(data) {
    // ensure the json data is a string
    if (
      data["comment"] &&
      !(
        typeof data["comment"] === "string" || data["comment"] instanceof String
      )
    ) {
      throw new Error(
        "Expected the field `comment` to be a primitive type in the JSON string but got " +
          data["comment"]
      );
    }

    return true;
  }
}

/**
 * Include project fleets in the report
 * @member {Boolean} app_fleets
 */
JobDefinitionReportOptions.prototype["app_fleets"] = undefined;

/**
 * Include project info in the report
 * @member {Boolean} app_info
 */
JobDefinitionReportOptions.prototype["app_info"] = undefined;

/**
 * Include project environment variables in the report
 * @member {Boolean} app_vars
 */
JobDefinitionReportOptions.prototype["app_vars"] = undefined;

/**
 * @member {String} comment
 */
JobDefinitionReportOptions.prototype["comment"] = undefined;

/**
 * Include device activity data in the report
 * @member {Boolean} device_activity
 */
JobDefinitionReportOptions.prototype["device_activity"] = undefined;

/**
 * Include device health data in the report
 * @member {Boolean} device_health
 */
JobDefinitionReportOptions.prototype["device_health"] = undefined;

/**
 * Include device info in the report
 * @member {Boolean} device_info
 */
JobDefinitionReportOptions.prototype["device_info"] = undefined;

/**
 * Include device environment variables in the report
 * @member {Boolean} device_vars
 */
JobDefinitionReportOptions.prototype["device_vars"] = undefined;

export default JobDefinitionReportOptions;
