/**
 * Notehub API
 * The OpenAPI definition for the Notehub.io API.
 *
 * The version of the OpenAPI document: 1.2.0
 * Contact: engineering@blues.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from "../ApiClient";

/**
 * The DescriptionRecord model module.
 * @module model/DescriptionRecord
 * @version 6.5.0
 */
class DescriptionRecord {
  /**
   * Constructs a new <code>DescriptionRecord</code>.
   * Metadata for a stored description record. The content itself is returned by GET on the record.
   * @alias module:model/DescriptionRecord
   * @param contentType {String} The stored content type, either \"application/json\" or \"text/plain; charset=utf-8\".
   * @param createdAt {Number} When the record was first created (Unix seconds).
   * @param length {Number} The content length in bytes.
   * @param md5 {String} The hex-encoded MD5 of the content.
   * @param modifiedAt {Number} When the record was last updated (Unix seconds).
   * @param name {String} The record name (letters, digits, '.', '_' or '-').
   * @param scope {String} The ownership scope of the record (\"global\" or \"project\").
   */
  constructor(contentType, createdAt, length, md5, modifiedAt, name, scope) {
    DescriptionRecord.initialize(
      this,
      contentType,
      createdAt,
      length,
      md5,
      modifiedAt,
      name,
      scope
    );
  }

  /**
   * Initializes the fields of this object.
   * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
   * Only for internal use.
   */
  static initialize(
    obj,
    contentType,
    createdAt,
    length,
    md5,
    modifiedAt,
    name,
    scope
  ) {
    obj["content_type"] = contentType;
    obj["created_at"] = createdAt;
    obj["length"] = length;
    obj["md5"] = md5;
    obj["modified_at"] = modifiedAt;
    obj["name"] = name;
    obj["scope"] = scope;
  }

  /**
   * Constructs a <code>DescriptionRecord</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:model/DescriptionRecord} obj Optional instance to populate.
   * @return {module:model/DescriptionRecord} The populated <code>DescriptionRecord</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      obj = obj || new DescriptionRecord();

      if (data.hasOwnProperty("content_type")) {
        obj["content_type"] = ApiClient.convertToType(
          data["content_type"],
          "String"
        );
      }
      if (data.hasOwnProperty("created_at")) {
        obj["created_at"] = ApiClient.convertToType(
          data["created_at"],
          "Number"
        );
      }
      if (data.hasOwnProperty("created_by")) {
        obj["created_by"] = ApiClient.convertToType(
          data["created_by"],
          "String"
        );
      }
      if (data.hasOwnProperty("length")) {
        obj["length"] = ApiClient.convertToType(data["length"], "Number");
      }
      if (data.hasOwnProperty("md5")) {
        obj["md5"] = ApiClient.convertToType(data["md5"], "String");
      }
      if (data.hasOwnProperty("modified_at")) {
        obj["modified_at"] = ApiClient.convertToType(
          data["modified_at"],
          "Number"
        );
      }
      if (data.hasOwnProperty("modified_by")) {
        obj["modified_by"] = ApiClient.convertToType(
          data["modified_by"],
          "String"
        );
      }
      if (data.hasOwnProperty("name")) {
        obj["name"] = ApiClient.convertToType(data["name"], "String");
      }
      if (data.hasOwnProperty("owner_uid")) {
        obj["owner_uid"] = ApiClient.convertToType(data["owner_uid"], "String");
      }
      if (data.hasOwnProperty("scope")) {
        obj["scope"] = ApiClient.convertToType(data["scope"], "String");
      }
    } else if (data === null) {
      return null;
    }
    return obj;
  }

  /**
   * Validates the JSON data with respect to <code>DescriptionRecord</code>.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DescriptionRecord</code>.
   */
  static validateJSON(data) {
    // check to make sure all required properties are present in the JSON string
    for (const property of DescriptionRecord.RequiredProperties) {
      if (!data[property]) {
        throw new Error(
          "The required field `" +
            property +
            "` is not found in the JSON data: " +
            JSON.stringify(data)
        );
      }
    }
    // ensure the json data is a string
    if (
      data["content_type"] &&
      !(
        typeof data["content_type"] === "string" ||
        data["content_type"] instanceof String
      )
    ) {
      throw new Error(
        "Expected the field `content_type` to be a primitive type in the JSON string but got " +
          data["content_type"]
      );
    }
    // ensure the json data is a string
    if (
      data["created_by"] &&
      !(
        typeof data["created_by"] === "string" ||
        data["created_by"] instanceof String
      )
    ) {
      throw new Error(
        "Expected the field `created_by` to be a primitive type in the JSON string but got " +
          data["created_by"]
      );
    }
    // ensure the json data is a string
    if (
      data["md5"] &&
      !(typeof data["md5"] === "string" || data["md5"] instanceof String)
    ) {
      throw new Error(
        "Expected the field `md5` to be a primitive type in the JSON string but got " +
          data["md5"]
      );
    }
    // ensure the json data is a string
    if (
      data["modified_by"] &&
      !(
        typeof data["modified_by"] === "string" ||
        data["modified_by"] instanceof String
      )
    ) {
      throw new Error(
        "Expected the field `modified_by` to be a primitive type in the JSON string but got " +
          data["modified_by"]
      );
    }
    // ensure the json data is a string
    if (
      data["name"] &&
      !(typeof data["name"] === "string" || data["name"] instanceof String)
    ) {
      throw new Error(
        "Expected the field `name` to be a primitive type in the JSON string but got " +
          data["name"]
      );
    }
    // ensure the json data is a string
    if (
      data["owner_uid"] &&
      !(
        typeof data["owner_uid"] === "string" ||
        data["owner_uid"] instanceof String
      )
    ) {
      throw new Error(
        "Expected the field `owner_uid` to be a primitive type in the JSON string but got " +
          data["owner_uid"]
      );
    }
    // ensure the json data is a string
    if (
      data["scope"] &&
      !(typeof data["scope"] === "string" || data["scope"] instanceof String)
    ) {
      throw new Error(
        "Expected the field `scope` to be a primitive type in the JSON string but got " +
          data["scope"]
      );
    }

    return true;
  }
}

DescriptionRecord.RequiredProperties = [
  "content_type",
  "created_at",
  "length",
  "md5",
  "modified_at",
  "name",
  "scope",
];

/**
 * The stored content type, either \"application/json\" or \"text/plain; charset=utf-8\".
 * @member {String} content_type
 */
DescriptionRecord.prototype["content_type"] = undefined;

/**
 * When the record was first created (Unix seconds).
 * @member {Number} created_at
 */
DescriptionRecord.prototype["created_at"] = undefined;

/**
 * The actor who created the record.
 * @member {String} created_by
 */
DescriptionRecord.prototype["created_by"] = undefined;

/**
 * The content length in bytes.
 * @member {Number} length
 */
DescriptionRecord.prototype["length"] = undefined;

/**
 * The hex-encoded MD5 of the content.
 * @member {String} md5
 */
DescriptionRecord.prototype["md5"] = undefined;

/**
 * When the record was last updated (Unix seconds).
 * @member {Number} modified_at
 */
DescriptionRecord.prototype["modified_at"] = undefined;

/**
 * The actor who last updated the record.
 * @member {String} modified_by
 */
DescriptionRecord.prototype["modified_by"] = undefined;

/**
 * The record name (letters, digits, '.', '_' or '-').
 * @member {String} name
 */
DescriptionRecord.prototype["name"] = undefined;

/**
 * The owning project (app) UID for project-scoped records; empty for global.
 * @member {String} owner_uid
 */
DescriptionRecord.prototype["owner_uid"] = undefined;

/**
 * The ownership scope of the record (\"global\" or \"project\").
 * @member {String} scope
 */
DescriptionRecord.prototype["scope"] = undefined;

export default DescriptionRecord;
