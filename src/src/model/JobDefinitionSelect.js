/**
 * Notehub API
 * The OpenAPI definition for the Notehub.io API.
 *
 * The version of the OpenAPI document: 1.2.0
 * Contact: engineering@blues.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from "../ApiClient";

/**
 * The JobDefinitionSelect model module.
 * @module model/JobDefinitionSelect
 * @version 6.4.0
 */
class JobDefinitionSelect {
  /**
   * Constructs a new <code>JobDefinitionSelect</code>.
   * Device selection criteria
   * @alias module:model/JobDefinitionSelect
   */
  constructor() {
    JobDefinitionSelect.initialize(this);
  }

  /**
   * Initializes the fields of this object.
   * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
   * Only for internal use.
   */
  static initialize(obj) {}

  /**
   * Constructs a <code>JobDefinitionSelect</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:model/JobDefinitionSelect} obj Optional instance to populate.
   * @return {module:model/JobDefinitionSelect} The populated <code>JobDefinitionSelect</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      obj = obj || new JobDefinitionSelect();

      if (data.hasOwnProperty("all_devices")) {
        obj["all_devices"] = ApiClient.convertToType(
          data["all_devices"],
          "Boolean"
        );
      }
      if (data.hasOwnProperty("comment")) {
        obj["comment"] = ApiClient.convertToType(data["comment"], "String");
      }
      if (data.hasOwnProperty("devices")) {
        obj["devices"] = ApiClient.convertToType(data["devices"], ["String"]);
      }
      if (data.hasOwnProperty("devices_by_sn")) {
        obj["devices_by_sn"] = ApiClient.convertToType(data["devices_by_sn"], [
          "String",
        ]);
      }
      if (data.hasOwnProperty("devices_in_fleets")) {
        obj["devices_in_fleets"] = ApiClient.convertToType(
          data["devices_in_fleets"],
          ["String"]
        );
      }
    } else if (data === null) {
      return null;
    }
    return obj;
  }

  /**
   * Validates the JSON data with respect to <code>JobDefinitionSelect</code>.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @return {boolean} to indicate whether the JSON data is valid with respect to <code>JobDefinitionSelect</code>.
   */
  static validateJSON(data) {
    // ensure the json data is a string
    if (
      data["comment"] &&
      !(
        typeof data["comment"] === "string" || data["comment"] instanceof String
      )
    ) {
      throw new Error(
        "Expected the field `comment` to be a primitive type in the JSON string but got " +
          data["comment"]
      );
    }
    // ensure the json data is an array
    if (!Array.isArray(data["devices"])) {
      throw new Error(
        "Expected the field `devices` to be an array in the JSON data but got " +
          data["devices"]
      );
    }
    // ensure the json data is an array
    if (!Array.isArray(data["devices_by_sn"])) {
      throw new Error(
        "Expected the field `devices_by_sn` to be an array in the JSON data but got " +
          data["devices_by_sn"]
      );
    }
    // ensure the json data is an array
    if (!Array.isArray(data["devices_in_fleets"])) {
      throw new Error(
        "Expected the field `devices_in_fleets` to be an array in the JSON data but got " +
          data["devices_in_fleets"]
      );
    }

    return true;
  }
}

/**
 * Select all devices in the project
 * @member {Boolean} all_devices
 */
JobDefinitionSelect.prototype["all_devices"] = undefined;

/**
 * @member {String} comment
 */
JobDefinitionSelect.prototype["comment"] = undefined;

/**
 * Specific device UIDs to include
 * @member {Array.<String>} devices
 */
JobDefinitionSelect.prototype["devices"] = undefined;

/**
 * Serial number patterns to match (supports glob wildcards *, ?, [...])
 * @member {Array.<String>} devices_by_sn
 */
JobDefinitionSelect.prototype["devices_by_sn"] = undefined;

/**
 * Fleet UIDs whose devices should be included
 * @member {Array.<String>} devices_in_fleets
 */
JobDefinitionSelect.prototype["devices_in_fleets"] = undefined;

export default JobDefinitionSelect;
