/**
 * Notehub API
 * The OpenAPI definition for the Notehub.io API.
 *
 * The version of the OpenAPI document: 1.2.0
 * Contact: engineering@blues.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from "../ApiClient";
import Note from "./Note";

/**
 * The Notefile model module.
 * @module model/Notefile
 * @version 4.0.0
 */
class Notefile {
  /**
   * Constructs a new <code>Notefile</code>.
   * @alias module:model/Notefile
   * @param id {String} Notefile id (e.g., \"test.qi\", \"config.db\").
   * @param notes {Array.<module:model/Note>}
   */
  constructor(id, notes) {
    Notefile.initialize(this, id, notes);
  }

  /**
   * Initializes the fields of this object.
   * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
   * Only for internal use.
   */
  static initialize(obj, id, notes) {
    obj["id"] = id;
    obj["notes"] = notes;
  }

  /**
   * Constructs a <code>Notefile</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:model/Notefile} obj Optional instance to populate.
   * @return {module:model/Notefile} The populated <code>Notefile</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      obj = obj || new Notefile();

      if (data.hasOwnProperty("id")) {
        obj["id"] = ApiClient.convertToType(data["id"], "String");
      }
      if (data.hasOwnProperty("notes")) {
        obj["notes"] = ApiClient.convertToType(data["notes"], [Note]);
      }
      if (data.hasOwnProperty("template")) {
        obj["template"] = ApiClient.convertToType(data["template"], "String");
      }
    } else if (data === null) {
      return null;
    }
    return obj;
  }

  /**
   * Validates the JSON data with respect to <code>Notefile</code>.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Notefile</code>.
   */
  static validateJSON(data) {
    // check to make sure all required properties are present in the JSON string
    for (const property of Notefile.RequiredProperties) {
      if (!data[property]) {
        throw new Error(
          "The required field `" +
            property +
            "` is not found in the JSON data: " +
            JSON.stringify(data)
        );
      }
    }
    // ensure the json data is a string
    if (
      data["id"] &&
      !(typeof data["id"] === "string" || data["id"] instanceof String)
    ) {
      throw new Error(
        "Expected the field `id` to be a primitive type in the JSON string but got " +
          data["id"]
      );
    }
    if (data["notes"]) {
      // data not null
      // ensure the json data is an array
      if (!Array.isArray(data["notes"])) {
        throw new Error(
          "Expected the field `notes` to be an array in the JSON data but got " +
            data["notes"]
        );
      }
      // validate the optional field `notes` (array)
      for (const item of data["notes"]) {
        Note.validateJsonObject(item);
      }
    }
    // ensure the json data is a string
    if (
      data["template"] &&
      !(
        typeof data["template"] === "string" ||
        data["template"] instanceof String
      )
    ) {
      throw new Error(
        "Expected the field `template` to be a primitive type in the JSON string but got " +
          data["template"]
      );
    }

    return true;
  }
}

Notefile.RequiredProperties = ["id", "notes"];

/**
 * Notefile id (e.g., \"test.qi\", \"config.db\").
 * @member {String} id
 */
Notefile.prototype["id"] = undefined;

/**
 * @member {Array.<module:model/Note>} notes
 */
Notefile.prototype["notes"] = undefined;

/**
 * @member {String} template
 */
Notefile.prototype["template"] = undefined;

export default Notefile;
