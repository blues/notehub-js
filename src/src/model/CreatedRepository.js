/**
 * Notehub API
 * The OpenAPI definition for the Notehub.io API.
 *
 * The version of the OpenAPI document: 1.2.0
 * Contact: engineering@blues.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from "../ApiClient";

/**
 * The CreatedRepository model module.
 * @module model/CreatedRepository
 * @version 6.4.0
 */
class CreatedRepository {
  /**
   * Constructs a new <code>CreatedRepository</code>.
   * @alias module:model/CreatedRepository
   */
  constructor() {
    CreatedRepository.initialize(this);
  }

  /**
   * Initializes the fields of this object.
   * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
   * Only for internal use.
   */
  static initialize(obj) {}

  /**
   * Constructs a <code>CreatedRepository</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:model/CreatedRepository} obj Optional instance to populate.
   * @return {module:model/CreatedRepository} The populated <code>CreatedRepository</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      obj = obj || new CreatedRepository();

      if (data.hasOwnProperty("fleet_uids")) {
        obj["fleet_uids"] = ApiClient.convertToType(data["fleet_uids"], [
          "String",
        ]);
      }
      if (data.hasOwnProperty("name")) {
        obj["name"] = ApiClient.convertToType(data["name"], "String");
      }
      if (data.hasOwnProperty("password")) {
        obj["password"] = ApiClient.convertToType(data["password"], "String");
      }
      if (data.hasOwnProperty("project_uids")) {
        obj["project_uids"] = ApiClient.convertToType(data["project_uids"], [
          "String",
        ]);
      }
      if (data.hasOwnProperty("uid")) {
        obj["uid"] = ApiClient.convertToType(data["uid"], "String");
      }
      if (data.hasOwnProperty("user")) {
        obj["user"] = ApiClient.convertToType(data["user"], "String");
      }
    } else if (data === null) {
      return null;
    }
    return obj;
  }

  /**
   * Validates the JSON data with respect to <code>CreatedRepository</code>.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CreatedRepository</code>.
   */
  static validateJSON(data) {
    // ensure the json data is an array
    if (!Array.isArray(data["fleet_uids"])) {
      throw new Error(
        "Expected the field `fleet_uids` to be an array in the JSON data but got " +
          data["fleet_uids"]
      );
    }
    // ensure the json data is a string
    if (
      data["name"] &&
      !(typeof data["name"] === "string" || data["name"] instanceof String)
    ) {
      throw new Error(
        "Expected the field `name` to be a primitive type in the JSON string but got " +
          data["name"]
      );
    }
    // ensure the json data is a string
    if (
      data["password"] &&
      !(
        typeof data["password"] === "string" ||
        data["password"] instanceof String
      )
    ) {
      throw new Error(
        "Expected the field `password` to be a primitive type in the JSON string but got " +
          data["password"]
      );
    }
    // ensure the json data is an array
    if (!Array.isArray(data["project_uids"])) {
      throw new Error(
        "Expected the field `project_uids` to be an array in the JSON data but got " +
          data["project_uids"]
      );
    }
    // ensure the json data is a string
    if (
      data["uid"] &&
      !(typeof data["uid"] === "string" || data["uid"] instanceof String)
    ) {
      throw new Error(
        "Expected the field `uid` to be a primitive type in the JSON string but got " +
          data["uid"]
      );
    }
    // ensure the json data is a string
    if (
      data["user"] &&
      !(typeof data["user"] === "string" || data["user"] instanceof String)
    ) {
      throw new Error(
        "Expected the field `user` to be a primitive type in the JSON string but got " +
          data["user"]
      );
    }

    return true;
  }
}

/**
 * @member {Array.<String>} fleet_uids
 */
CreatedRepository.prototype["fleet_uids"] = undefined;

/**
 * repository name
 * @member {String} name
 */
CreatedRepository.prototype["name"] = undefined;

/**
 * read-only password for the database, also used as X-Repository-Token header for subsequent API calls.  This value is only served once when the repository is created
 * @member {String} password
 */
CreatedRepository.prototype["password"] = undefined;

/**
 * @member {Array.<String>} project_uids
 */
CreatedRepository.prototype["project_uids"] = undefined;

/**
 * The unique identifier for the data repository
 * @member {String} uid
 */
CreatedRepository.prototype["uid"] = undefined;

/**
 * read-only user for database
 * @member {String} user
 */
CreatedRepository.prototype["user"] = undefined;

export default CreatedRepository;
